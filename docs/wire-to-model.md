# Wire format to model correspondence

Generated by `scripts/gen_wire_doc.py` from `tools/wire.py`, which is the
single source of truth for the layout. Offsets are accumulated from field
lengths and never written by hand, so a corrected length cannot leave a stale
offset behind it. `scripts/verify_wire_table.py` cross-checks every cited
model term against `models/proverif/lib/wireguard_core.pvl` and fails the
build if the two disagree.

Layout follows the WireGuard whitepaper (Donenfeld, NDSS 2017), section 5.4.

## Why the omissions are listed

The fields a model leaves out are as much a part of its correspondence to
reality as the fields it includes, and they are the part most often skipped.
Each omission below carries its reason. Two are worth singling out.

**`mac2` and the cookie mechanism** are load-shedding devices. A Dolev-Yao
attacker has no notion of computational cost, so no symbolic model can express
the property they provide. It is argued in the report instead: `mac1` is keyed
by `HASH(LABEL_MAC1 ‖ S_r)`, so an attacker ignorant of the responder's static
public value cannot produce a message that survives the first check, and
cannot force a Diffie-Hellman operation. A real property, genuinely outside
the model.

**The `mac1` of the response** is keyed by the *initiator's* static public
value. An observer holding a candidate public key can test whether a given
response is addressed to its holder. WireGuard's identity hiding is therefore
conditional: it protects the initiator from an observer who does not already
hold the candidate keys, and not from one who does. This is visible only
because `mac1` was modelled explicitly rather than abstracted away.

## `handshake_initiation` — 148 bytes

| Offset | Length | Field | Encrypted | Model term |
|---:|---:|---|---|---|
| 0 | 1 | `message_type` | — | *not modelled* |
| 1 | 3 | `reserved` | — | *not modelled* |
| 4 | 4 | `sender_index` | — | *not modelled* |
| 8 | 32 | `unencrypted_ephemeral` | — | `Ei = pk(ei)` |
| 40 | 48 | `encrypted_static` | yes | `msgStatic = aead(k1, 0, g2b(Si), hi1)` |
| 88 | 28 | `encrypted_timestamp` | yes | `msgTs = aead(k2, 0, tai64n, hi2)` |
| 116 | 16 | `mac1` | — | `mac(hash(concat(LABEL_MAC1, g2b(Sr))), ...)` |
| 132 | 16 | `mac2` | — | *not modelled* |

**Fields not represented in the models**

- `message_type` — Constant 1. Demultiplexing only; the models use distinct channels.
- `reserved` — Zero on transmission, ignored on receipt. Carries no security burden.
- `sender_index` — Session demultiplexing. Not modelled: it is chosen freely by the sender and the protocol's security does not depend on its value.
- `mac2` — Cookie-dependent, all-zero when the responder is not under load. The cookie mechanism is a rate-limiting device outside the Dolev-Yao model, which has no notion of computational load; argued in the report instead.


## `handshake_response` — 92 bytes

| Offset | Length | Field | Encrypted | Model term |
|---:|---:|---|---|---|
| 0 | 1 | `message_type` | — | *not modelled* |
| 1 | 3 | `reserved` | — | *not modelled* |
| 4 | 4 | `sender_index` | — | *not modelled* |
| 8 | 4 | `receiver_index` | — | *not modelled* |
| 12 | 32 | `unencrypted_ephemeral` | — | `Er = pk(er)` |
| 44 | 16 | `encrypted_nothing` | yes | `msgEmpty = aead(k3, 0, EMPTY, hi5)` |
| 60 | 16 | `mac1` | — | `mac(hash(concat(LABEL_MAC1, g2b(Si))), ...)` |
| 76 | 16 | `mac2` | — | *not modelled* |

**Fields not represented in the models**

- `message_type` — Constant 2. Demultiplexing only; the models use distinct channels.
- `reserved` — Zero on transmission, ignored on receipt. Carries no security burden.
- `sender_index` — Responder's session index. Chosen freely; carries no security burden.
- `receiver_index` — Echoes the initiator's sender_index.
- `mac2` — Cookie-dependent, as in message 1. Not modelled for the same reason: the symbolic attacker has no notion of computational load.


## `cookie_reply` — 64 bytes

| Offset | Length | Field | Encrypted | Model term |
|---:|---:|---|---|---|
| 0 | 1 | `message_type` | — | *not modelled* |
| 1 | 3 | `reserved` | — | *not modelled* |
| 4 | 4 | `receiver_index` | — | *not modelled* |
| 8 | 24 | `nonce` | — | *not modelled* |
| 32 | 32 | `encrypted_cookie` | yes | *not modelled* |

**Fields not represented in the models**

- `message_type` — Constant 3. Demultiplexing only; the models use distinct channels.
- `reserved` — Zero on transmission, ignored on receipt. Carries no security burden.
- `receiver_index` — Echoes the sender's index. Demultiplexing only, no security burden.
- `nonce` — XChaCha20-Poly1305 nonce.
- `encrypted_cookie` — Not modelled: the cookie is a load-shedding mechanism, and the symbolic attacker has no notion of computational cost.


## `transport_data_header` — 16 bytes

| Offset | Length | Field | Encrypted | Model term |
|---:|---:|---|---|---|
| 0 | 1 | `message_type` | — | *not modelled* |
| 1 | 3 | `reserved` | — | *not modelled* |
| 4 | 4 | `receiver_index` | — | *not modelled* |
| 8 | 8 | `counter` | — | *not modelled* |

**Fields not represented in the models**

- `message_type` — Constant 4. Demultiplexing only; the models use distinct channels.
- `reserved` — Zero on transmission, ignored on receipt. Carries no security burden.
- `receiver_index` — Selects the session. Demultiplexing only; the handshake models cover key agreement, not the data plane.
- `counter` — Nonce counter, also the anti-replay window index. The data plane's sliding window is outside the handshake models.
